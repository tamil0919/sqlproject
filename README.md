Designed and implemented a relational database using MySQL.

Applied multiple SQL constraints including PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, CHECK, and DEFAULT.

Inserted meaningful data (10–15 records per table) and ensured data integrity through relationships.

Used JOINs, subqueries, aggregate functions, and SQL operators to retrieve and analyze data.

Created views, indexes, and stored procedures to optimize performance and reusability.
